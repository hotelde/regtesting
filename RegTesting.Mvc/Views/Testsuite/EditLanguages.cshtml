@using RegTesting.Mvc.Models
@model IList<int>

@{
	ViewBag.Title = "Edit Testsuite - RegTesting";
	var languages = ((List<LanguageModel>) ViewBag.LstLanguages);
}

<h2>Edit Testsuite - Set languages</h2>
<fieldset>
	<legend>Sprachen</legend>
	<form method="post" action="@Url.Action("LanguagesChanged")">

	
		<input type = "hidden" name = "testsuite" value = "@ViewBag.Testsuite" />

		@foreach (LanguageModel languageModel in languages)
		{
			if (!Model.Contains(languageModel.ID))
			{
				<input type = "checkbox" name = "languages" value = "@languageModel.ID"/>
				@languageModel.Name
				<br/>
			}
			else
			{
				<input type = "checkbox" name = "languages" value = "@languageModel.ID" checked="checked" />
				@languageModel.Name
				<br/>
			}
		}
		<br/>
		<input type="submit" value="Save"/>
	</form>
</fieldset>


<div>
	@Html.ActionLink("Zurück", "Edit", new {id = ViewBag.Testsuite})
</div>